[project]
name = "servicenow-mcp-poc"
version = "0.1.0"
description = "Read-only ServiceNow MCP server for Pod Charlie DIT use cases (incident, KB, runbook, post-mortem, SLA, CAB)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]<2",
    "httpx",
    "python-dotenv",
]

[tool.uv]
package = false
